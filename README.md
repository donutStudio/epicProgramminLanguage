In the program directory, enter:
    `python main.py test.language`
to run the file `test.language` in my programming language. You can create other .language files too. The `example_fp.language` file will not
work when you run it, as it has some features this interpreter does not support yet